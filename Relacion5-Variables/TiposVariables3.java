/*Ejercicio 3 Convierte en expresiones algorítmicas con Java las siguientes expresiones algebraicas. Coloca
paréntesis solamente donde sean necesarios.

1ºDAW | Javier Muñoz Parra */

public class TiposVariables3{
    public static void main ( String args[]){
        /* a) */
        double sumafrac = 3/2+4/3;

        /* b) */
        double restaFracComp = 1/(x-5) - 3*x*y/4;

        /* c) */
        double sumafrac2 = 1/2 + 7;

        /* d) */
        double sumafrac3 = 7 + 1/2;

        /* e) */
        double sumafrac4 = (a*a/(b - c) + (d - e)/(f-g*h/j));

        /* f) */
        double sumafrac5 = m/n + p 

        /* g) */
        double sumafrac6 =  m + n/(p - q)

        /* h) */
        double sumafrac7 = a * a/(b * b) + c * c / (d*d)

        /* i) */
        double sumafrac8 = (m + n/p) /(q -r/s)

        /* j) */ 
        double sumafrac9 = (3a + b)/(c - (d + 5e /(f + g/2*h)));

        /* k) */
        double sumafrac10 = (a* a + 2 * a * b)/(1/(x * x))+2;
    }
}
